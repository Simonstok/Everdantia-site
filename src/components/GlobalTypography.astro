---
// Global Typography Styles for Enhanced Readability
// Applies across all pages for consistent, accessible text
---

<style is:global>
  /* Base typography for optimal readability */
  html {
    font-size: 16px; /* Base size for rem calculations */
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    font-size: 1.125rem; /* 18px - optimal reading size */
    line-height: 1.6; /* Relaxed line height for body text */
    color: #2d4a22; /* High contrast text */
    background: #f8f6f2;
    font-feature-settings: 'liga' 1, 'kern' 1; /* Typography features */
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Headings with improved hierarchy */
  h1, h2, h3, h4, h5, h6 {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-weight: 700;
    line-height: 1.25; /* Tighter for headings */
    margin-bottom: 0.75em;
    color: #6b4f2a;
    letter-spacing: -0.02em; /* Slight negative spacing for large text */
  }

  h1 {
    font-size: clamp(2.5rem, 5vw, 4rem); /* Responsive scaling */
    margin-bottom: 1rem;
  }

  h2 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 0.875rem;
  }

  h3 {
    font-size: clamp(1.5rem, 3vw, 1.875rem);
    margin-bottom: 0.75rem;
  }

  h4 {
    font-size: 1.25rem;
    margin-bottom: 0.625rem;
  }

  h5, h6 {
    font-size: 1.125rem;
    margin-bottom: 0.5rem;
  }

  /* Paragraph and body text optimization */
  p {
    font-size: 1.125rem; /* 18px minimum for readability */
    line-height: 1.6;
    margin-bottom: 1.25em;
    max-width: 65ch; /* Optimal reading width */
    color: #2d4a22;
  }

  /* Long-form content (articles, legal pages) */
  .prose {
    font-size: 1.125rem;
    line-height: 1.7; /* Extra spacing for long reads */
    max-width: 65ch;
  }

  .prose p {
    margin-bottom: 1.5em;
  }

  .prose h2 {
    margin-top: 2em;
    margin-bottom: 1em;
  }

  .prose h3 {
    margin-top: 1.5em;
    margin-bottom: 0.75em;
  }

  /* Lists with proper spacing */
  ul, ol {
    font-size: 1.125rem;
    line-height: 1.6;
    margin-bottom: 1.25em;
    padding-left: 1.5em;
    max-width: 65ch;
  }

  li {
    margin-bottom: 0.5em;
    color: #2d4a22;
  }

  li::marker {
    color: #6b4f2a;
  }

  /* Links with accessible focus states */
  a {
    color: #6b4f2a;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 2px;
    transition: all 0.2s ease;
  }

  a:hover {
    color: #4a3419;
    text-decoration-thickness: 2px;
  }

  a:focus {
    outline: 2px solid #8bab5d;
    outline-offset: 2px;
    border-radius: 2px;
  }

  /* Improved text contrast for different contexts */
  .text-secondary {
    color: #4d6b3c;
    font-size: 1rem;
  }

  .text-muted {
    color: #6b7f5c;
    font-size: 0.875rem;
  }

  .text-inverse {
    color: #ffffff;
  }

  /* Card content readability */
  .card-content p {
    font-size: 1rem;
    line-height: 1.5;
    max-width: 50ch; /* Shorter for card context */
  }

  .card-title {
    font-size: 1.5rem;
    line-height: 1.3;
    margin-bottom: 0.75rem;
  }

  /* Small text that's still readable */
  .text-small {
    font-size: 0.875rem; /* 14px minimum */
    line-height: 1.5;
  }

  /* Caption and metadata text */
  .text-caption {
    font-size: 0.875rem;
    line-height: 1.4;
    color: #6b7f5c;
  }

  /* Mobile readability improvements */
  @media (max-width: 768px) {
    body {
      font-size: 1rem; /* Slightly smaller on mobile */
      line-height: 1.5;
    }

    p {
      font-size: 1rem;
      line-height: 1.5;
      margin-bottom: 1em;
    }

    h1 {
      font-size: 2rem;
      line-height: 1.2;
    }

    h2 {
      font-size: 1.75rem;
      line-height: 1.3;
    }

    h3 {
      font-size: 1.5rem;
    }

    /* Ensure touch targets are large enough */
    button, input, select, textarea {
      min-height: 44px; /* Minimum touch target */
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    body {
      color: #000000;
    }

    h1, h2, h3, h4, h5, h6 {
      color: #000000;
    }

    a {
      color: #0000EE;
    }

    a:visited {
      color: #551A8B;
    }
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    * {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }

  /* Print styles for better readability */
  @media print {
    body {
      font-size: 12pt;
      line-height: 1.4;
      color: #000000;
    }

    h1, h2, h3, h4, h5, h6 {
      color: #000000;
      page-break-after: avoid;
    }

    p {
      orphans: 3;
      widows: 3;
    }
  }
</style>
